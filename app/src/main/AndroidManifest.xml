<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.robert.tool">



    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:name=".MyApplication"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.image.SelectPhotoActivity"/>
        <activity android:name=".view.webview.JSWebViewActivity"/>
        <activity android:name=".view.refresh.RefreshActivity"/>
        <activity android:name=".view.qrcode.QRCodeActivity"/>
        <activity android:name=".view.greendao.GreenDaoActivity"/>
        <activity android:name=".view.snap.SnapHorizontalActivity"/>


    </application>

</manifest>